# Variables para Spring Boot + PostgreSQL (Supabase pooler)
# Copia este archivo a .env y reemplaza los valores.

DB_URL=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
DB_USERNAME=postgres.cyohgfityjrccegxuftb
DB_PASSWORD=TU_PASSWORD_AQUI
SPRING_PROFILES_ACTIVE=prod

# Opcional en local (Render lo inyecta automaticamente)
# PORT=8080
